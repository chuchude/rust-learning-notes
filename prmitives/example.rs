fn main() {
    let subject = "World";
    let greeting = format!("Hello,{}!", subject); //string interpolation

    println!("hello world");
    //panic!("program crashed! {}", crash_reason); //panic! is to stop the program
    //this line will never be run
    //println!("This will never be run");
}
